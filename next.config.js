/** @type {import('next').NextConfig} */
const nextConfig = {
  /*
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'xy.com'
      }
    ]
  }
  */
};

module.exports = nextConfig;
